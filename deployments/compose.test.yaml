services:
  app:
    image: ${IMAGE}
    restart: unless-stopped
    ports:
      - ${HOST_PORT}:8080  # Direct access for devices
    volumes:
      - pixel-config:/app/etc
  ui:
    image: node:22-alpine
    restart: unless-stopped
    user: ${DOCKER_UGID}
    working_dir: /home/node/web
    command: npm run dev --debug
    ports:
      - 8082:4000  # Vite dev server for HMR
    volumes:
      - type: bind
        source: ${BUILDROOT}
        target: /home/node
volumes:
  pixel-config: {}
